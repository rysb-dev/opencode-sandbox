# =============================================================================
# OpenCode Sandbox Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp env.example .env
#
# NEVER commit .env to version control - it contains secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# Required: Project Directory
# -----------------------------------------------------------------------------
# The directory to mount as the workspace inside the sandbox.
# This is the only directory the agent can access.
PROJECT_DIR=/path/to/your/project

# -----------------------------------------------------------------------------
# Required: LLM API Keys (at least one)
# -----------------------------------------------------------------------------
# Anthropic Claude API key
ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI API key (optional, if using OpenAI models)
# OPENAI_API_KEY=sk-xxxxx

# -----------------------------------------------------------------------------
# Optional: Git Configuration for Push/Pull
# -----------------------------------------------------------------------------
# Personal Access Token for GitHub (or other git host)
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public only)
# GIT_TOKEN=ghp_xxxxx

# Git username (defaults to 'git' which works for most hosts)
# GIT_USER=git

# Git host (defaults to github.com)
# GIT_HOST=github.com

# -----------------------------------------------------------------------------
# Optional: Mount Host OpenCode Configuration
# -----------------------------------------------------------------------------
# If you have opencode configured on your host machine and want to reuse
# that configuration (including any auth settings), set these paths.
# The container will import these on startup (read-only).
#
# On macOS/Linux, these are typically:
#   OPENCODE_HOST_CONFIG=~/.config/opencode
#   OPENCODE_HOST_CACHE=~/.cache/opencode
#
# Leave commented out to start fresh with container-only config.
# OPENCODE_HOST_CONFIG=/Users/yourname/.config/opencode
# OPENCODE_HOST_CACHE=/Users/yourname/.cache/opencode
